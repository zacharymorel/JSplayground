/**
 * Can be a singly or doubly linked list.
 * Properties on Data structure:
 *  value
 *  next?
 *  prev
 *
 * A stack is backwards to a queue.
 * We don't need a doubly linked list.
 * Power of a stack is how constraining it is when mutating data.
 * First In LAST out.
 *
 * A <- B <- C <- D
 * Tail           Head
 *
 * Operations for setting points is constant time.
 * Big O = O(1);
 *
 * Example: https://github.com/zacharymorel/kata-machine/blob/3bf6245de9748cee74a833baea1ff66792845a6b/src/day1/Queue.ts
 */
